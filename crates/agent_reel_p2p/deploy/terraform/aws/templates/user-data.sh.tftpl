#!/usr/bin/env bash
set -euxo pipefail

export DEBIAN_FRONTEND=noninteractive

apt-get update
apt-get install -y --no-install-recommends \
  build-essential \
  ca-certificates \
  caddy \
  curl \
  git \
  jq \
  libssl-dev \
  pkg-config \
  unzip

if ! command -v aws >/dev/null 2>&1; then
  tmpdir="$(mktemp -d)"
  curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "$tmpdir/awscliv2.zip"
  unzip -q "$tmpdir/awscliv2.zip" -d "$tmpdir"
  "$tmpdir/aws/install" --bin-dir /usr/local/bin --install-dir /usr/local/aws-cli --update
  rm -rf "$tmpdir"
fi

if ! id agent-reel >/dev/null 2>&1; then
  useradd --system --home-dir /var/lib/agent-reel --create-home --shell /usr/sbin/nologin agent-reel
fi

install -d -m 0755 /etc/agent-reel
install -d -m 0750 -o agent-reel -g agent-reel /var/lib/agent-reel
install -d -m 0755 /usr/local/bin

cat >/etc/agent-reel/edge.env <<'EOF_EDGE_ENV'
${edge_env}
EOF_EDGE_ENV
github_client_id="$(aws ssm get-parameter --name "${github_client_id_parameter}" --with-decryption --query Parameter.Value --output text 2>/dev/null || true)"
github_client_secret="$(aws ssm get-parameter --name "${github_client_secret_param}" --with-decryption --query Parameter.Value --output text 2>/dev/null || true)"
if [ -n "$github_client_id" ]; then
  printf 'AGENT_FEED_GITHUB_CLIENT_ID=%s\n' "$github_client_id" >>/etc/agent-reel/edge.env
fi
if [ -n "$github_client_secret" ]; then
  printf 'AGENT_FEED_GITHUB_CLIENT_SECRET=%s\n' "$github_client_secret" >>/etc/agent-reel/edge.env
fi
chmod 0640 /etc/agent-reel/edge.env
chown root:agent-reel /etc/agent-reel/edge.env

cat >/etc/agent-reel/edge.toml <<'EOF_EDGE_TOML'
${edge_toml}
EOF_EDGE_TOML
chmod 0644 /etc/agent-reel/edge.toml

cat >/etc/caddy/Caddyfile <<'EOF_CADDY'
${caddyfile}
EOF_CADDY

cat >/etc/systemd/system/agent-reel-edge.service <<'EOF_SERVICE'
${edge_service_unit}
EOF_SERVICE

if [ "${agent_reel_install_source}" = "crate" ]; then
  if [ ! -x /root/.cargo/bin/cargo ]; then
    curl https://sh.rustup.rs -sSf | sh -s -- -y --profile minimal
  fi
  export HOME=/root
  export CARGO_HOME=/root/.cargo
  export RUSTUP_HOME=/root/.rustup
  # shellcheck disable=SC1091
  source /root/.cargo/env
  cargo install --locked agent_reel_cli --version "${agent_reel_crate_version}" --bin agent-reel
else
  if [ ! -x /root/.cargo/bin/cargo ]; then
    curl https://sh.rustup.rs -sSf | sh -s -- -y --profile minimal
  fi
  export HOME=/root
  export CARGO_HOME=/root/.cargo
  export RUSTUP_HOME=/root/.rustup
  # shellcheck disable=SC1091
  source /root/.cargo/env
  cargo install \
    --locked \
    --git "${agent_reel_git_repository}" \
    --branch "${agent_reel_git_ref}" \
    --bin agent-reel \
    agent_reel_cli
fi

ln -sf /root/.cargo/bin/agent-reel /usr/local/bin/agent-reel

systemctl daemon-reload
systemctl enable caddy
systemctl restart caddy
systemctl enable agent-reel-edge
systemctl restart agent-reel-edge
